import { UrlSeguraPipe } from './url-segura.pipe';

describe('UrlSeguraPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlSeguraPipe();
    expect(pipe).toBeTruthy();
  });
});
